# Local Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=quiz_management
DB_USER=root
DB_PASSWORD=your_local_password

# Authentication
JWT_SECRET=replace_with_secure_secret

# Server Configuration
PORT=5000
FRONTEND_URL=http://localhost:3000

# Optional: Cloud Database URL (Overrides DB_HOST/PORT/USER/PASSWORD/NAME if set)
# DATABASE_URL=mysql://avnadmin:password@mysql-example.aivencloud.com:17052/defaultdb?ssl-mode=REQUIRED
